[project]
name = "welfare-state-democracy-panel"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "pandas==3.0.3",
    "loguru==0.7.3",
    "numpy==2.4.6",
    "pydantic>=2.0",
]

[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"
